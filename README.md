# pw3-ingresso
Matheus Guerino da Silva e Victor Matheus Silva Sisnande

Banco de dados da sessão:
CREATE TABLE TBL_SESSAO (
ID_SESSAO BIGINT PRIMARY KEY IDENTITY,
NR_DATA DATE,
NR_HORARIO TIME,
ID_FILME BIGINT,
FOREIGN KEY (ID_FILME) REFERENCES TBL_FILME(ID_FILME),
NR_PRECO FLOAT,
TX_SALA VARCHAR(50) NOT NULL
);